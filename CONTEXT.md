# 프로젝트 컨텍스트 (개발 일지 겸 의사결정 기록)

이 문서는 다른 세션(로컬 Claude Code든, 다른 웹 세션이든)이 대화 맥락 없이도
"지금까지 뭘 왜 이렇게 만들었는지"를 파악할 수 있게 정리한 기록이다.
빌드/실행 방법은 `README.md` 참고. 여긴 **왜 이런 구조가 됐는지**를 담는다.

## 콘테스트 개요

2P GAME ARCADE 「1.44MB GAME_DEV CONTEST」 출품작.
- 배포 패키지(실행파일+에셋) 전체가 1,474,560 bytes(1.44MB) 이하
- 인터넷/서버/브라우저 없이 독립 실행되는 실행파일이어야 함
- 마감: 2026-09-04 23:59 (KST)
- 공식 발표 이후 새로 제작한 원작이어야 함

## 기술 스택 선택 이유

- **순수 Win32 API + GDI 소프트웨어 렌더링, 외부 런타임 0** — SDL 등 프레임워크를 쓰면
  런타임만으로 수십 MB라 예산이 안 나옴. 정적 링크 + `-Os` + strip으로 실행파일을
  최소화하는 방향으로 처음부터 고정.
- **mingw-w64 크로스컴파일** — 개발 환경(리눅스 컨테이너)에서 Windows용 exe를 직접
  뽑아낸다. `build.sh`가 이 작업을 담당.
- 지금 실행파일 크기: **~26KB** (예산 1.44MB 대비 압도적으로 여유 있음 — 병목은
  이미지/오디오 에셋 쪽이 될 것으로 예상, 아직 안 넣음)

## 장르가 여러 번 바뀐 이유 (중요 — 같은 삽질 반복 방지용)

1. **텍스트 비주얼노벨** (분기형 심리 스릴러, 파킨슨병 환자 + 옛 친구의 복수극) —
   스토리/분기 구조/떡밥까지 다 설계했었는데, 실제로 대사를 텍스트로 읽어보니
   "플레이하는 느낌"이 없어서 재미 판단이 어려웠음. **폐기.**
2. **웨이브 슈팅** — VN 폐기 직후 바로 프로토타입. 조작감 자체는 됐지만 다음 아이디어로 대체.
3. **포병 대결(각도+파워 쏘기)** — Scorched Earth류. 컨셉상 재밌었지만 유저가
   "포병처럼 실제 절차를 재현하고 싶다"는 더 구체적인 아이디어를 제시하면서 전환.
4. **→ 현재: K9 자주포 수동 사격 절차 게임** — 전장은 안 보이고 포반원 시점에서
   하달받은 제원대로 빠르고 정확하게 방열하는 게임. 이후 계속 다듬어서 지금 형태로 정착.

## 현재 게임 디자인

**장르**: 밀(Mil) 단위 2단계 수동 방열 시뮬레이션.
FDC가 불러주는 방위각/사각 수치에 맞춰 방열하고, 스페이스로 확정 → 오차 합산으로 최종 판정.

### 흐름
`로비` → `인트로`(대사, ESC/Space로 스킵) → `조작화면`(방위각 → 페이드 → 사각) → 결과 화면 → 로비

### 조작화면 — 1단계: 방위각 (회전 다이얼)
- 실제 포병 단위 그대로 사용: 원 한 바퀴 = **6400밀** (`MIL_FULL`)
- 좌클릭 드래그로 회전 — **기어비 적용, 최대 20바퀴** 돌려야 전체 범위 커버
  (`AZ_GEAR_TURNS = 20`). 처음엔 1바퀴=6400밀이었다가 "여러 바퀴 돌리는 손맛"을
  원해서 기어비를 넣었고, 10 → 20으로 한 번 더 늘림.
- 우클릭으로 미세조정: 클릭 1번 = **1밀** (`AZ_FINE_STEP_MIL`). 바늘의 **현재 각도
  기준**으로 시계방향 클릭=감소, 반시계=증가 (화면상 좌/우로 판단하면 바늘 방향에
  따라 결과가 뒤바뀌는 버그가 있었음 — 수정됨).

### 조작화면 — 2단계: 사각 (세로 릴, 화살표 고정)
- 범위: 조정 가능 0~1244밀, 실제 목표값은 300~1100밀에서 랜덤 (`EL_TARGET_MIN/MAX`).
  스펙상 실제 범위는 -44~1244밀이지만 마이너스 부호 렌더링이 없어서 이번엔 스킵.
- 눈금 **1밀 단위**로 표시 (`EL_TICK_STEP = 1`, `EL_PX_PER_MIL = 24`) — 화면엔 현재값
  주변 10여 개만 확대되어 보임 (촘촘한 자 느낌).
- 좌클릭 드래그로 스크롤, **아래로 끌면 증가** (반대 방향으로 한 번 뒤집음).
  드래그 민감도 1밀/px (`EL_DRAG_MIL_PER_PX`) — 처음 4였다가 "살짝만 끌어도 확
  튀는" 문제로 4배 완화.
- **관성(휴대폰 슬라이드 스타일)**: 손 뗀 뒤에도 관성으로 미끄러지다 마찰로 감속
  (`EL_FRICTION`, `elVelocity`).
- 드래그 시작 판정 영역이 원래 좁아서(70px 원형, 다이얼과 공용) 릴 전용으로
  넓은 사각형 히트박스로 분리함 (`EL_GRAB_HALF_W/H`).
- 우클릭 미세조정 1밀/클릭.

### 말풍선 (좌측, 두 스테이지 공통 유지)
FDC가 목표값을 자릿수 단위로 하나씩 불러주는 연출 (예: 방위각 2100이면 "2...1...0...0").
**핑크 배경째로 잠깐 사라졌다가 다음 자릿수가 나옴** (자릿수 사이 공백 필수 —
처음엔 곧바로 다음 숫자로 넘어가서 "끊어서 나온다"는 원래 의도와 다르다는 피드백 있었음).
4자리(밀 단위라 3자리였다가 4자리로 늘림). 한 바퀴 다 불러주면 긴 침묵 후 반복.
`BUBBLE_PHASE_DUR` 배열 참고.

### 확정 / 판정
- 각 스테이지에서 **스페이스**로 현재 값을 확정 (정확하지 않아도 확정됨 — 자동
  통과 없음. 원래는 허용오차 안에 들어오면 자동으로 다음 단계로 넘어갔는데
  "판단하기도 전에 통과되는 게 어색하다"는 피드백으로 스페이스 확정 방식으로 변경).
- 확정 시 해당 스테이지의 오차(목표와의 밀 단위 절댓값)를 기록.
- 두 스테이지 다 확정하면 오차 합산 → **`TOTAL_ERROR_SUCCESS_MAX`(5밀) 이하면 명중**.
- 결과 화면 3종 (전부 화면 중앙 큰 글씨):
  - **TARGET HIT** (초록) — 명중
  - **MISSION FAILED** (빨강) — 오차 초과
  - **SQUAD WIPED** (빨강, 검정배경) — 60초(`GAMEPLAY_TIME_LIMIT`) 타임오버, 조작 불가
- 스테이지 전환은 항상 **검정 페이드아웃 → (다음 스테이지면) 페이드인** 연출.
  타이머는 전환 중에도 계속 흐름 (단, 스페이스로 확정한 직후엔 막판에 타임오버가
  결과를 덮어쓰지 않도록 가드 있음).

### 로비 / 옵션 / 인트로
- 로비: START/OPTIONS/EXIT 버튼 (마우스 클릭+호버).
- 옵션: 오버레이로 뜸. SOUND ON/OFF, LANGUAGE EN→KR→JP 순환 — **순환만 되고
  실제 한/일 번역 텍스트는 아직 없음** (아래 "다음 할 일" 참고).
- 인트로: 대사칸 + 영문 placeholder 텍스트, Space/클릭/ESC로 스킵.

## 엔진 아키텍처 메모

- `src/main.c` 하나에 전부 있음 (의도적 — 빌드 단순화, 파일 분리는 굳이 안 함).
- **씬 상태머신**: `SCENE_LOBBY / OPTIONS / INTRO / GAMEPLAY`, `UpdateGame`/`RenderGame`이
  switch로 분기.
- **입력 처리 관련 버그였던 것 (재발 방지용 기록)**:
  1. 마우스 좌표는 창이 `WINDOW_SCALE`(2배)로 확대되어 있어서 반드시 게임 좌표계로
     나눠줘야 함 (`WM_MOUSEMOVE`에서 `/ WINDOW_SCALE`).
  2. "방금 눌림" 엣지 판정(`mouseDown && !mouseDownPrev`)은 `keysPrev`/`mouseDownPrev`
     스냅샷을 **프레임 맨 처음, 메시지 처리 전**에 떠야 함. `UpdateGame` 끝에서
     갱신했더니 `RenderGame` 쪽(로비 버튼 등)에서는 항상 거짓으로 나오는 버그가 있었음.
- **텍스트 렌더링**: 폰트 엔진 없음. 두 가지 직접 구현:
  - 7세그먼트 숫자 (`DrawDigit`/`DrawNumber`) — 큰 수치 표시용
  - 5x7 도트매트릭스 폰트 (`DrawChar`/`DrawLabel`, `FONT5X7` 테이블) — 영문 라벨용,
    A-Z + 일부 기호만 구현됨. **한글/일본어는 이 방식으로 불가** (조합형 한글만
    11,172자) — 필요해지면 문구를 이미지로 미리 그려서 언어별로 갈아끼우는
    파이프라인이 별도로 필요함 (아직 구현 안 함).
  - 참고: 함수명을 `DrawText`로 지었다가 `windows.h`의 `DrawTextA` 매크로와
    충돌해서 `DrawLabel`로 개명함.
- **알파 블렌딩**: `StretchDIBits`/`BI_RGB`는 블렌딩을 안 해줘서, 페이드 효과는
  `FillPixelRectAlpha`로 프레임버퍼에 직접 수동 블렌딩.
- **프레임 페이싱**: Windows 기본 타이머 해상도(~15ms)에서 `Sleep(1)`이 실제로는
  훨씬 오래 자서 드래그 중 프레임이 낮아 보이는 문제가 있었음.
  `timeBeginPeriod(1)`/`timeEndPeriod(1)` (winmm 링크 필요)로 해결.
- **이미지 로더는 아직 없음** — 지금 화면의 배경/버튼/타이틀 등은 전부 색깔로만
  구분한 사각형(placeholder). 실제 아트 넣으려면 BMP 로더+블릿 함수 추가 필요
  (설계는 논의됐음: 무압축 인덱스 BMP로 가되 예산 빠듯해지면 RLE 압축 고려).

## 다음 할 일 (우선순위 순 아님, 논의된 것들)

- [ ] 로비 외 나머지 화면(인트로/조작화면/옵션) 배경 아트 — BMP 로더는 이미 있으니
      같은 방식으로 파일만 추가하면 됨 (아래 "BMP 로더" 항목 참고)
- [ ] BGM 멜로디/편곡 다듬기 — 지금은 16스텝 단선율 베이스라인 하나뿐(아래
      "BGM" 항목 참고). 필요하면 2보이스로 늘리거나 스테이지별로 다른
      루프를 두는 것도 가능(같은 GenerateMusicLoop 방식 재사용).
- [ ] 일본어 문구 — 한국어/영어는 `T()`로 전환되는데 일본어(langIndex==2)는 아직
      미번역이라 한국어로 폴백함(아래 "영문 전환 시스템" 항목 참고)
- [ ] 가스계량기 스타일 원통형 릴 비주얼 (지금은 평평한 텍스트 스크롤 — 곡률
      왜곡 효과는 나중 폴리싱으로 미룸)
- [ ] 커스텀 커서/3단계 게이지/사각 오브젝트/4단계 원반·통 전부 색깔 사각형·원
      placeholder — 실제 아트로 교체 필요
- [ ] **"스테이지 넘어갈때 폰트는 미적용이어서 어색" 미해결** — 로비/옵션/인트로/
      방위각/사각/사격/장약 전 화면을 스크린샷으로 재확인했지만 HBIOS-SYS가
      전부 정상 적용된 상태로 재현이 안 됨(2026-08-16 세션). 사용자가 본
      정확한 화면/타이밍 스크린샷을 받아서 다시 조사 필요.
- [ ] **새 한글 문구를 `src/main.c`에 추가할 때마다 폰트 서브셋 재생성 필요**
      (아래 "커스텀 폰트" 항목 참고) — 안 하면 새 글자가 안 보임(폴백 없이
      그냥 안 그려짐, 시스템 폰트로 자동 대체되지 않음). 영문은 서브셋에 ASCII
      전체가 이미 들어있어서 재생성 불필요, `T("한국어", "English")`만 추가하면 됨.
      **서브셋을 다시 뽑을 땐 반드시 `--no-prune-codepage-ranges
      --no-prune-unicode-ranges` 옵션을 같이 줘야 함** (아래 "폰트 미적용
      버그" 항목 참고 — 안 그러면 폰트가 조용히 시스템 폰트로 폴백됨).

## 볼드체 되돌림 + 명중음 덕킹/소프트클립 재설계 (2026-08-16, 같은 날 여섯 번째)

- **볼드체 되돌림**: `g_fontSmall`/`g_fontMedium`을 `FW_BOLD`→`FW_NORMAL`로
  되돌림("볼드체는 하지말자"). `g_fontLarge`는 이번 세션에서 건드린 적 없는
  원래부터의 BOLD라 그대로 둠(결과 화면 "명중"/"임무 실패" 등).
- **"스테이지 넘어갈때 폰트 미적용" 조사**: 로비/옵션/인트로(한국어)/방위각/
  사각/사격/장약 7개 화면 전부, 그리고 스테이지 간 실제 전환(스페이스로
  확정 → 페이드 → 다음 스테이지)까지 임시 디버그 훅(숫자키로 스테이지
  강제 이동)으로 스크린샷 재확인했지만 전부 HBIOS-SYS가 정상 적용된
  상태였음 — 재현 실패. 검은 화면이 몇 번 잡혔는데 전부 캡처 타이밍
  문제(대화 지연으로 실제 시간이 흘러 미션 타이머/결과 홀드가 이미 끝나
  로비로 돌아가 있었거나, 순수 페이드 전환 중 캡처된 것)였고 실제 폰트
  버그는 아니었음. 원인 미상 상태로 "다음 할 일"에 남겨둠 — 정확히 어느
  화면/타이밍에서 봤는지 스크린샷을 받아야 재조사 가능.
- **명중음이 커져도 안 들리는 문제**: 두 가지를 의심하고 둘 다 고침.
  1) 순수 곱셈 후 하드클램프(0~255로 자르기) 방식이라 게인을 3배 올리면
     파형이 각지게 잘려서(디지털 클리핑) 오히려 저음 바디가 뭉개져 "약하고
     지지직대는" 소리가 될 수 있음 — `tanhf` 소프트새추레이션으로 교체(부드럽게
     눌러서 더 크고 뭉툭한 펀치감).
  2) **더 유력한 원인**: 전장 앰비언스가 게임플레이 내내(3단계 포함) 빈틈없이
     깔려 있어서, 명중음을 아무리 키워도 앰비언스에 묻혔을 가능성이 큼.
     `waveOutSetVolume`으로 명중음이 울리는 동안(~180ms) 전장 앰비언스
     채널 볼륨을 잠깐 낮추는 "덕킹"을 추가(`DuckWarAmbience`, `g_warDuckTimer`
     로 매 프레임 카운트다운 후 원상복구 — `UpdateWarLoop`가 이제 `dt`를
     받도록 시그니처 변경). waveOutSetVolume은 버퍼를 새로 만들 필요 없이
     재생 중인 소리의 출력 볼륨만 즉시 바꿔주므로 이런 짧은 덕킹에 적합함.
  덤으로 바디 주파수도 110→145Hz, 배음 950→1100Hz로 살짝 올려서 전장
  앰비언스의 저음 위주 폭발음 대역과 덜 겹치게 함(주파수 대역 분리로
  마스킹 완화).
- **검증**: 임시 키(H)로 명중음을 반복 재생시켜 크래시 없음 확인. 오디오
  자체(볼륨이 실제로 커졌는지, 덕킹이 체감되는지)는 여전히 자동화로 들어볼
  수 없어서 사용자 재확인 필요.

## 폰트 미적용 버그 수정 + 한글 자간 타이트하게 + 명중음 3배 증폭 (2026-08-16, 같은 날 다섯 번째)

- **"HBIOS-SYS가 미적용됨" 버그 원인**: `pyftsubset`의 기본 동작
  (`--prune-codepage-ranges`, `--prune-unicode-ranges`)이 서브셋 후 남은
  글자 수를 기준으로 OS/2 테이블의 `ulCodePageRange1`에서 **한글(Wansung
  949) 비트를 자동으로 꺼버림**(서브셋엔 107자만 남아서 "이 폰트는 한글 지원
  안 함"으로 재계산). `CreateFontW`에 `HANGUL_CHARSET`을 넘기면 GDI가 이
  비트로 폰트를 매칭하는데, 비트가 꺼져 있으면 이름이 정확히 일치해도
  **에러 없이 조용히** 시스템 폰트(맑은 고딕)로 대체됨 — 그래서 겉보기엔
  "폰트가 하나도 안 바뀐 것처럼" 보였음. `fonts/HBIOS-SYS.ttf`를
  `--no-prune-codepage-ranges --no-prune-unicode-ranges`로 재추출해서 해결.
  이후 새로 서브셋을 뽑을 땐 항상 이 두 옵션을 같이 줘야 함(위 "다음 할 일"에
  기록해둠).
- **한글 자간이 너무 넓어 보이는 문제**: HBIOS-SYS는 터미널 폰트 관례상 한글
  음절의 advance width가 라틴 문자의 정확히 2배(1em 정사각 칸, 실측:
  라틴 800유닛 vs 한글 1600유닛, unitsPerEm=1600)라 그대로 쓰면 음절
  사이가 헐렁해 보임. `SetTextCharacterExtra`로 한국어 텍스트일 때만
  (`langIndex != 1 && g_customFontLoaded`) 폰트 크기별 음수 자간을 줘서
  타이트하게 당김.
  - **부수적으로 발견한 진짜 버그**: 자간을 세게 줄수록(-14→-18 등) 짧은
    문자열(옵션 버튼, 6~7자)은 멀쩡한데 긴 문자열(로비 "아무키나 눌러서
    시작", 10자)은 음절이 서로 겹쳐서 뭉개짐. 원인은 `DrawTextW(DT_CENTER)`가
    가운데 정렬을 위해 내부적으로 폭을 재는 단계와 실제 그리는 단계가 자간
    설정을 다르게 반영해서, 문자열이 길어질수록 두 계산이 어긋나 누적
    드리프트가 커지는 것으로 추정됨. **해결**: `DrawTextW(DT_CENTER)`
    대신 `GetTextExtentPoint32W`로 직접 폭을 측정한 뒤 `TextOutW`로 수동
    가운데 정렬 — 측정/출력에 항상 같은 자간 설정이 적용되므로 문자열
    길이와 무관하게 항상 정확함. `FlushTextQueue`에서 `c->w > 0` 분기만
    교체.
  - **최종 자간 값** (`korCharExtra[3]`, fontIdx 0/1/2 = 작게(-18)/중간(-26)/
    크게(-56) 폰트 크기에 대응): `{ -14, -30, -30 }`. 작은 폰트는 픽셀이
    적어서 중간/큰 폰트와 같은 비율로 줄이면 바로 겹침 — 실측(스크린샷)으로
    개별 보정함. 폴백 폰트(맑은 고딕)이거나 영어 모드일 때는 이 폭 차이가
    없어서 자간을 건드리지 않음(0).
- **3단계 명중음 볼륨 3배**: "타격감이 약하다"는 피드백으로 최종 출력
  게인을 80→240으로 증폭(레이어 믹스 비율은 그대로, 최종 스케일만 3배).
  8비트 PCM이라 피크 부분은 자연히 더 클리핑되는데, 타격음 특성상 오히려
  펀치감에 도움이 됨.
- **검증**: 폰트 수정 후 옵션/로비/1단계(방위각) 전 화면을 스크린샷으로
  재확인. 자간 튜닝은 "값 하나 바꾸고 스크린샷 → 여전히 이상함 → 더
  바꾸고 스크린샷"을 여러 번 반복하다가, 짧은 문자열과 긴 문자열의 결과가
  다르다는 패턴을 보고서야 진짜 원인(DrawTextW 버그)을 찾음 — **교훈**: 자간
  같은 텍스트 레이아웃 버그는 반드시 길이가 다른 여러 문자열로 교차
  검증해야 함, 한 종류 문자열(예: 옵션 버튼)만 보고 "됐다"고 판단하면
  다른 문자열(로비 문구)에서 숨어있던 버그를 놓침.

## 영문 전환 시스템 + 폰트 볼드체 + 3단계 명중음 교체 (2026-08-16, 같은 날 네 번째)

- **영문 전환**: 기존 `Kor(utf8)`(UTF-8 → UTF-16, 4칸 회전버퍼) 위에
  `T(kor, eng)` 한 겹만 얹음 — `langIndex==1`이면 영문을, 아니면 한국어를
  그대로 `Kor()`에 넘김. 영문도 순수 ASCII라 UTF-8로 취급해도 문제없어서
  별도 변환 경로 없이 기존 Kor() 재사용. 일본어(langIndex==2)는 아직 미번역
  상태라 T()가 자동으로 한국어로 폴백 — 옵션에서 "일본어"를 선택해도 화면
  텍스트는 그대로 한국어인 게 의도된 동작(추후 일본어 문구를 채우면 `langIndex
  == 2` 분기만 추가하면 됨, 구조는 이미 갖춰짐).
  모든 `Kor("리터럴")` 호출을 `T("한국어","English")`로 교체했고, `wsprintfA`로
  조립되는 동적 문구(옵션의 "사운드: 켬/끔", "언어: %s", 장약의 "장약 %d
  키로!!", "합 %d")는 `langIndex==1` 분기로 영문 포맷 문자열을 따로 조립.
  폰트 서브셋에 이미 ASCII 전체가 포함돼 있어서(로고/폰트 세션에서 향후
  영문 대비로 미리 넣어둠) 이번엔 폰트 재작업 없이 바로 적용됨.
- **볼드체**: `g_fontSmall`/`g_fontMedium`을 `FW_NORMAL`→`FW_BOLD`로 변경
  (`g_fontLarge`는 원래부터 BOLD). HBIOS-SYS는 별도 Bold 웨이트가 없는
  단일 굵기 폰트지만, GDI가 `FW_BOLD` 요청 시 알고리즘적으로 획을 굵게
  합성(emboldening)해주므로 폰트 파일 교체 없이 그대로 적용됨.
- **3단계 명중음 교체**: 기존엔 500→1200Hz로 상승하는 사인 스윕이라 "캐쥬얼한
  차임"처럼 들린다는 피드백. 망치로 때린 느낌으로 바꾸기 위해 3레이어로
  재설계: ①노이즈 트랜지언트(감쇠 45배속, ~10ms — 타격 순간 "탁")
  ②110Hz 사인 바디(감쇠 6배속, ~60~80ms — 망치머리가 때리는 둔탁한 저음
  "퉁", 이번 사운드의 핵심) ③950Hz 짧은 배음(감쇠 20배속, 아주 조용히 15%
  비중만 — 쇠붙이 느낌만 살짝). 기존 함수 구조(같은 `g_hitBuf`/`g_tickHdr`
  버퍼와 채널 재사용)는 그대로 두고 파형 생성 로직만 교체.
- **검증**: 폰트/영문 전환은 옵션에서 언어 버튼 클릭 → 로비/인트로/1단계
  화면 스크린샷으로 전부 확인(레이아웃 안 깨지고 텍스트 잘 들어감,
  "Grip and Turn", "Drag: Coarse Adjust" 등). 명중음은 오디오라 자동화로
  들어볼 수 없어서, 파형 생성 코드가 버퍼 경계 안에서 안전한지 리뷰하고
  기존 통과된 실행 경로(언어 전환 테스트가 이미 여러 씬을 거쳐감)로 크래시
  여부만 확인.

## 커스텀 한글 폰트(HBIOS-SYS) 적용 (2026-08-16, 같은 날 세 번째)

- **배경**: "폰트를 주면 인식 가능해?"로 시작. 사용자가 `HBIOS-SYS.ttf`(5.6MB,
  완성형 한글 11172자 전체 + ASCII 포함, BIOS/터미널 스타일)를 제공.
  `fontTools`로 name 테이블 조회해서 라이선스 확인(CC-BY-SA 4.0 + SIL OFL 1.1,
  재배포/임베드 허용) 후 진행.
- **서브셋**: `src/main.c` 안의 `Kor("...")` 리터럴 전부 + `wsprintfA`로
  조립되는 동적 문구(옵션의 "사운드: 켬/끔", "언어: %s", 장약의 "장약 %d
  키로!!", "합 %d" 등)까지 합쳐서 실제 화면에 그려지는 문자 집합을 뽑고, 여기에
  향후 영어 로컬라이즈 대비로 ASCII 전체를 더해서 `pyftsubset`으로 추출 →
  **5.6MB → 33KB**. `fonts/LICENSE-HBIOS-SYS.txt`에 재추출 명령어를 정확히
  적어둠 — 다음에 새 한글 문구가 추가되면 그 문서의 커맨드를 그대로 재실행하면
  됨(주의: 지금 서브셋에 없는 글자는 폴백 없이 그냥 안 보이므로 잊지 말 것).
- **검증 방법**: 실제 게임에 넣기 전에 Pillow로 서브셋 폰트를 직접 로드해서
  게임 문구들을 렌더링한 미리보기 PNG를 만들어 사용자에게 먼저 확인받음
  (전체 파이프라인을 다 연결하기 전에 폰트 자체의 느낌과 글자 커버리지를
  저렴하게 검증하는 방법 — BGM 때 WAV 미리듣기로 먼저 확인받았던 것과 같은
  패턴).
- **연동**: 폰트 설치 없이 실행 중에만 등록하는 `AddFontMemResourceEx`를 씀
  (관리자 권한 불필요, 프로세스 종료 시 자동 해제되지만 `WM_DESTROY`
  경로에서 `RemoveFontMemResourceEx`로도 명시적으로 해제). 로드는 다른
  에셋들과 동일하게 exe 옆 `fonts/HBIOS-SYS.ttf`를 런타임에 읽어서 처리
  (`GetAssetPath` 재사용). `CreateFontW`의 `lpszFaceName`을 폰트 파일의
  실제 내부 family name("HBIOS-SYS")으로 지정해야 골라 쓸 수 있음. 로드
  실패 시(폰트 파일 없음/손상) 기존 "맑은 고딕" 시스템 폰트로 자동 폴백.
- **용량 영향**: 342KB → 380KB (33KB 증가), 여전히 1.44MB 예산에 크게 여유.

## 4단계(장약) 말풍선 타이밍을 표시-대기 2단계로 단순화 (2026-08-16, 같은 날 두 번째)

- **요청**: 장약 콜아웃은 말풍선이 뜰 때마다 음성도 같이 나와야 하는데, 기존
  8단계 깜빡임 구조로는 한 사이클에 4번씩 깜빡여서(그때마다 소리내면 너무
  시끄러움) 사이클당 한 번만 소리내는 절충안을 썼었음. 사용자가 대신 "말풍선
  뜰 때마다 소리 내되, 표시 1초 유지 → 사라짐 → 3초 대기 → 다시 표시+소리"
  라는 훨씬 단순한 타이밍으로 명확히 지정.
- **구현**: 방위각/사각이 쓰는 8단계 `BUBBLE_PHASE_DUR` 배열은 그대로 두고,
  장약 전용 배열만 `BUBBLE_PHASE_DUR_CHARGE[8]`(문장이 4번 깜빡이는 구조)에서
  `BUBBLE_PHASE_DUR_CHARGE[CHARGE_BUBBLE_PHASE_COUNT=2] = {1.0, 3.0}`로
  교체. phase 증가 시 쓰는 모듈러 값(`% 8`)도 장약일 때만 `% 2`를 쓰도록
  분기(`isChargeStage` 지역변수로 판단). 렌더링 쪽 `bubblePhase % 2 == 0` 조건은
  원래도 짝수=표시였어서 그대로 재사용 가능했고, `TriggerBubbleVoice`의
  `if (phase == 0) PlayVoiceNumber(...)` 조건도 그대로 재사용됨 — 2단계
  체제에서는 phase가 0으로 돌아올 때마다(=매번 다시 나타날 때마다) 자동으로
  소리도 같이 남.
- **검증**: 임시 디버그 훅으로 장약 스테이지 진입 후 일정 간격으로 캡처해서
  "표시(1초)→숨김(3초)" 전환이 실제로 일어나는지 확인(표시↔숨김 전환 자체는
  스크린샷으로 명확히 확인됨). 테스트 중 한 번 프로세스가 원인 불명으로
  조용히 종료된 사례가 있었는데, 재실행 시 18초 연속 무크래시로 재현이 안 돼서
  테스트 하네스 쪽의 일회성 문제로 판단(코드 로직 자체는 방위각/사각에서 이미
  검증된 것과 동일한 패턴이라 위험 낮음). 디버그 훅은 확인 후 제거.

## 무전 콜아웃 보이스(실제 육성 가공) 추가 (2026-08-16, 새 세션 첫 번째)

- **배경**: "8비트 효과음으로 원 투 쓰리 발음 구현할 수 있어?"로 시작. 순수 코드
  합성(사인파/포먼트)만으로 사람 말소리를 재현하는 건 사실상 불가능하다고 판단해서,
  실제 목소리를 녹음받아 가공하는 방식을 제안 → 사용자가 `녹음 (2).m4a`에
  "1,2,3,...,9,0" 순서로 숫자 10개를 녹음해서 제공.
- **자동 분리**: ffmpeg로 16kHz mono wav 변환 후, 20ms 윈도우 RMS 엔벨로프로
  무음 구간을 찾아 자동 세그멘테이션(짧은 갭 병합 + 짧은 블립 제거) → 정확히
  10개 구간 검출, 순서대로 1~0에 매핑. 자릿수 사전 지식(정확히 10개, 이 순서로
  말했다는 사용자 진술) 덕분에 별도 음성인식 없이도 안전하게 분리 가능했음.
- **음색 튜닝 (여러 라운드 시행착오)**: 처음엔 무전대역 밴드패스(300~3400Hz)만
  살짝 입혔는데 "사람 생목소리 같다"는 피드백 → 밴드패스+소프트클리핑(드라이브
  3.5)+5비트 크러시로 세게 갔더니 "괴물목소리 같다"는 반응이 나왔고, 사용자가
  오히려 그 방향을 원한다고 확인("원형을 못알아볼정도로 변형") → 피치를
  9세미톤 다운(재생속도를 늦추는 방식이라 포먼트도 같이 낮아짐, 클래식한
  "느리게 재생한 괴물목소리" 원리) + 한 옥타브 더 낮춘 서브레이어 믹스 +
  38Hz 저주파 링모듈레이션 + 드라이브 4.5 왜곡 + 4비트 크러시까지 갔더니
  "너무 갔다"는 피드백 → medium(피치6/링모듈0.28/왜곡2.6/6비트)으로 완화 →
  "medium과 원본 사이 정도로" 요청받아 모든 파라미터를 medium과 무가공 원본의
  정확히 중간값으로 재계산(피치3세미톤/링모듈0.14/왜곡1.8/7비트)해서 최종 승인.
  **교훈**: 음성 가공처럼 주관적 취향이 강한 작업은 파라미터를 조금씩 조정한
  버전을 매번 실제로 들려주고 확인받는 게 맞음 — 설명만으로는 "괴물목소리"의
  강도 차이를 가늠하기 어려움.
- **파이프라인**(최종 확정본, `process()` 함수 파라미터): 피치다운 3세미톤(재생
  속도 조절 방식) → 6세미톤 다운 서브레이어를 11%만 믹스 → 42Hz 링모듈레이션
  (깊이 0.14) → 220~3200Hz 밴드패스(order 2) → tanh 소프트클리핑(드라이브 1.8)
  → 8000Hz로 다운샘플 → 7비트(128단계) 비트크러시 → 옅은 화이트노이즈(0.006) →
  피크 90% 정규화 → 8비트 unsigned PCM.
- **에셋 포맷**: `audio/voice_digits.bin` — (uint32 길이 + PCM데이터) x 10, 배열
  인덱스=숫자값(0~9) 순서. BMP 로더처럼 exe 옆 `audio/`에서 런타임에 읽음
  (`LoadVoiceDigits`). `build.sh`가 `audio/*.bin`을 `dist/audio/`로 복사.
  전체 10개 합쳐도 ~33KB라 1.44MB 예산에 전혀 부담 없음.
- **재생**: 기존 SFX들과 마찬가지로 8bit/8000Hz PCM, 전용 `g_waveOutVoice` 채널
  하나 더 열어서 다른 사운드와 안 끊기게 함. 여러 자리 숫자(예: 장약 "18")를
  이어 읽어야 해서, `waveOutWrite`를 여러 헤더로 연달아 호출하면 드라이버가
  큐에 쌓아서 순서대로 자동으로 이어 재생해준다는 점을 활용(`PlayVoiceDigits`,
  최대 4자리까지 큐잉 가능). 재생 중 새로 호출되면 `waveOutReset`으로 먼저
  끊고 새 시퀀스를 큐잉 — 틱/명중음과 동일한 패턴.
- **게임 연동**: 방위각/사각은 기존에 있던 말풍선 자릿수 콜아웃 로직(8단계
  `bubblePhase`, 짝수=자릿수 표시)에 그대로 얹었음 — `TriggerBubbleVoice(phase)`를
  phase가 바뀌는 두 지점(치직 인트로 플래시가 끝나고 phase=0으로 시작할 때,
  그리고 매 phase 전환마다)에서 호출해서 화면에 뜨는 숫자와 정확히 동기화되게
  함. 장약은 문장 전체가 반복 깜빡이는 방식이라, 깜빡일 때마다 매번 다시 읽지
  않고 한 사이클(phase==0)에 한 번만 두 자리를 이어 읽게 함(안 그러면 한
  사이클에 4번씩 반복돼서 시끄러움).
- **검증**: 자동화 테스트로는 오디오 자체를 들을 수 없어서, 방위각/사각 실제
  진행 + 장약 스테이지는 임시 디버그 훅(`EnterGameplay` 안에서 강제로
  `FIRESTAGE_CHARGE` 진입, TEMP-TEST-DEBUG 마커로 표시 후 확정 완료해서 제거)으로
  크래시 여부만 확인. 음질 자체는 위 시행착오 과정에서 매 라운드 실제 WAV
  미리듣기를 사용자에게 전달해서 직접 확인받는 방식으로 검증함.

## 로고 위치 조정 + 레버 틱음 재트리거 완화 (2026-08-15, 같은 날 일곱 번째 세션)

- **로고 위치**: `LOGO_Y`를 8→18로, 10px 아래로 이동(요청 그대로).
- **레버/릴 틱음 문제**: `PlayTick()`은 항상 고정 950Hz로 재생되는데, 1밀 넘어갈
  때마다(`azLastTickMil`/`elLastTickMil` 비교) 매번 `waveOutReset` 후 처음부터
  재생하는 구조라 빠르게 돌리면 감쇠 전에(`TICK_SAMPLES=25ms`) 계속 재트리거됨 —
  이 빠른 재시작 자체가 원래 음보다 높은 배음처럼 들리는 원인. 실제로 주파수를
  올리는 코드는 없고, 순전히 재트리거 빈도가 만들어내는 착청임.
- **해결**: 사용자가 제안한 대로 "10단위마다만 재생"으로 변경. 새 상수
  `TICK_MIL_STEP = 10`을 두고, 밀 값을 그 값으로 나눈 몫이 바뀔 때만
  `PlayTick()` 호출(방위각 1곳, 사각 3곳 — 드래그 중/관성 슬라이드 중 총 4개
  트리거 지점 전부 동일하게 수정). 결과적으로 초당 재트리거 횟수 자체가
  1/10로 줄어서 겹침 문제가 자연 해소됨(재생 주기가 완전히 소멸시간보다
  넉넉히 길어짐).

- **배경**: 로비 배경(`Lobby.bmp`)은 8비트 인덱스 BMP로 잘 됐는데, "HANASAM" 로고는
  텍스트 주변에 투명 배경이 필요해서 같은 방식이 안 통함. 순수 GDI/무외부라이브러리
  제약 안에서 진짜 알파 채널 BMP를 파싱하는 대신, **크로마키**(마젠타 등 특정 색을
  "이 픽셀은 그리지 마라"로 취급)로 처리하기로 함 — 이미 지직 SFX 등에서 쓰던 "코드로
  직접 다 처리한다"는 방향과 일관됨.
- **로고 파일 포맷 이슈**: 사용자가 준 `images/Logo.bmp`(304x61)가 포토샵에서 이번엔
  **16비트**로 내보내졌음(8비트 인덱스가 없어서 그런 듯). 기존 `LoadBmpInto`는
  8/24비트만 지원해서 16비트 디코딩(X1R5G5B5, 비트마스크 없는 BI_RGB 기본 포맷 —
  5비트씩 추출 후 `(v5<<3)|(v5>>2)`로 8비트 근사)을 추가함.
- **로더 일반화**: 기존 `LoadBmpInto`는 배경 전용이라 `GAME_W x GAME_H`(640x360)와
  정확히 일치해야만 로드됨. 로고 같은 스프라이트는 크기가 다르므로 `expectW==0`이면
  "파일에 적힌 크기 그대로, 단 `maxW/maxH` 이내"로 로드하고 실제 크기를
  `outW/outH`로 돌려주는 모드를 추가(오버플로 방지). 배경 로딩 콜사이트는 그대로
  `expectW=GAME_W, expectH=GAME_H`로 호출하고, 로고는 `expectW=0,
  maxW=LOGO_MAX_W(400), maxH=LOGO_MAX_H(100)`로 호출.
- **합성**: `BlitSpriteAlpha(src, w, h, x, y, keyColor, alpha)` 신규 함수 — 마젠타
  픽셀은 완전히 건너뛰고, 나머지는 `alpha` 비율로 프레임버퍼와 선형 블렌딩. alpha를
  0→1로 애니메이션시키면 그대로 페이드인이 됨.
- **페이드인 + 4px 상승 애니메이션**: "로비 진입 시 로고가 페이드인하면서 살짝
  아래(4px)에서 위로 올라옴" 요청 반영. `UpdateGame`에서 `scene`이
  `SCENE_LOBBY`/`SCENE_OPTIONS`로 "막" 전환된 프레임에만 `lobbyLogoTimer`를 0으로
  리셋(`lobbyWasActive` 플래그로 감지) — 옵션 팝업을 열고 닫아도 로비 자체를
  벗어난 게 아니므로 로고 애니메이션이 다시 재생되지 않음(의도된 동작, 로비→인트로
  →로비처럼 실제로 로비를 재진입할 때만 재생). `LOGO_ANIM_TIME=0.45초` 동안
  `progress=timer/ANIM_TIME`를 0~1로 클램프해서 alpha와 y오프셋(`RISE_PX*(1-progress)`)
  둘 다에 사용.
- **배치**: 참고 이미지대로 좌상단(`LOGO_X=15, LOGO_Y=8`)에 배치, 기존 "게임 타이틀"
  텍스트+보라색 placeholder 박스는 로고 로드 실패시에만 폴백으로 남겨둠(배경
  로드 여부에 따라 딤 오버레이 유무도 기존 로직 그대로 유지).
- **검증**: PrintWindow 캡처로 로고가 마젠타 없이 정상 합성되는지, 옵션 팝업을 열어도
  로고가 딤 처리된 배경 위에서 자연스럽게 남아있고 텍스트 번짐이 없는지 확인함.

## BGM 칩튠 합성 추가 (2026-08-15, 같은 날 다섯 번째 세션)

- **방식 결정**: 짧은 루프 WAV 파일 vs 코드로 직접 합성, 두 옵션을 제시했고
  칩튠 합성 쪽으로 결정됨 — 용량이 거의 0이고("지직" 잡음 SFX랑 같은 원리),
  레트로 군사무전 컨셉과도 어울림.
- **구조**: SFX(`g_waveOut`)와는 별도로 `g_waveOutMusic` 핸들을 하나 더 열어서
  (`InitMusic`) 지직 잡음이랑 동시에 나도 서로 안 끊김. `GenerateMusicLoop`이
  16스텝(8분음표, 100BPM) A단조 저음 베이스라인을 8비트/8000Hz PCM 버퍼
  하나로 미리 다 렌더링해두고, 매 프레임 `UpdateMusicLoop`이 폴링하면서
  버퍼가 다 끝나면(`WHDR_DONE`) 같은 버퍼를 다시 큐에 넣는 방식으로 무한
  루프시킴(waveOut 자체엔 반복 재생 기능이 없어서 수동으로 구현).
  `soundOn`이 꺼지면 `waveOutReset`으로 즉시 멈춤.
- **배열 크기 계산 주의**: 스텝 샘플 수를 처음에 `MUSIC_BPM`을 float로 두고
  계산했더니 GCC가 "variably modified at file scope" 경고를 냄 — 부동소수
  상수 연산은 배열 크기(정수 상수 표현식) 문맥에서 컴파일 타임에 확실히
  안 접힐 수 있음. `MUSIC_BPM`을 정수로 바꾸고 순수 정수 나눗셈으로만
  계산하도록 고쳐서 해결(8000*60/100/2 = 2400, 나누어떨어져서 정밀도 손실도 없음).
- **후속: 전장/행군 느낌 보강**: 단선율 베이스만으로는 밋밋해서, 사용자가
  "전장 느낌"을 요청 → 3레이어로 확장. ①베이스 오스티나토(기존) ②킥 —
  스텝 0/4/8/12(4분음표 강박)마다 피치가 90Hz→40Hz로 빠르게 떨어지는 사인
  버스트(대포 타격감) ③스네어 — 스텝 2/6/10/14(뒷박)마다 짧은 노이즈
  버스트. "쿵-탁-쿵-탁" 행진 리듬이 되도록 배치. 세 레이어를 한 샘플 버퍼에
  그냥 더해서(clamp만 함) 믹싱 — 별도 채널이나 믹서 없이 `GenerateMusicLoop`
  안에서 레이어별로 조건 체크하며 `mix`에 누적.
- **음량/음역대**: 진폭 22(효과음의 절반 이하 수준)로 배경음답게 작게,
  55~98Hz 저음 위주라 존재감은 있되 거슬리지 않는 걸 목표로 함.
- **용량 영향**: 오디오 데이터를 통째로 실행파일에 넣는 게 아니라 코드로만
  생성해서 exe가 33KB→34KB로만 늘어남 (음악 자체는 런타임에 RAM에서만
  38.4KB로 생성됨, 배포 용량엔 안 잡힘).
- **후속: 참고 트랙 분석 후 재작곡** — 사용자가 `war.mp3`(Suno로 만든 21초짜리
  전쟁 테마)를 주면서 "이 느낌으로" 요청. 멜로디를 그대로 옮기는 건 저작권 +
  콘테스트 "원작이어야 함" 규정 둘 다에 걸려서 안 하고, **기술적 특징만 분석**해서
  새로 작곡하는 쪽으로 진행:
  - ffmpeg(winget으로 설치) + python(numpy/scipy pip 설치)로 mp3를 모노 wav로
    변환 후 분석. 자기상관 기반 온셋 템포 추정 → 약 92 BPM. 모노포닉 피치
    검출은 실패(폴리포닉 믹스라 당연함) → STFT 기반 크로마(피치클래스 에너지
    분포)로 대체 → G/A/D/Bb/C가 상위, D#(Eb)가 최하위 → **G 도리안**(자연
    6도가 살아있는 단조)으로 해석. 구간별 RMS로 다이내믹도 확인 → 곡 전체에
    걸쳐 꾸준히 커짐(고조 구조).
  - 이 세 가지(템포 92, G 도리안, 고조 다이내믹)만 반영해서 리드/베이스
    멜로디를 처음부터 새로 작성. 마디별 진폭 배율(`MUSIC_BAR_VOLUME`
    0.70→0.85→1.00→1.15)을 추가해서 참고 트랙처럼 루프가 갈수록 세지다가
    다시 처음(약하게)으로 돌아가는 파도치는 구조를 만듦.
  - 분석에 쓴 임시 파일(wav, python 스크립트)은 프로젝트 밖 작업 폴더에서
    처리하고 다 지움 — 저장소에는 안 남음.
  - **후속: BPM 72로 더 늦추고 리드 멜로디 한 옥타브 내림** — "낮고 느리게"
    피드백 반영. 킥 주파수도 65→25Hz로 더 낮춤. 베이스는 이미 낮은 음역대라
    그대로 둠(더 내리면 작은 스피커에서 거의 안 들릴 위험).
- **후속: BGM은 로비 전용, 게임 중엔 전쟁 앰비언스로 교체** — "로비에서만
  BGM, 게임 시작 후엔 전장 소리(폭발+총성), 미션 성공 시 정지" 요청 반영:
  - `UpdateMusicLoop`에 씬 조건 추가 — `SCENE_LOBBY`/`SCENE_OPTIONS`가 아니면
    `waveOutReset`으로 즉시 멈춤.
  - `g_waveOutWar`라는 세 번째 waveOut 핸들을 열어(BGM/SFX와 독립) 12초짜리
    전쟁 앰비언스 루프를 재생. 구성: 옅은 브라운노이즈 배경음 + 낙하 휘파람
    (1300→200Hz 사인 스윕) 뒤에 이어지는 저음+노이즈 붐(폭발) x3회 + 자동
    사격처럼 여러 발이 연달아 나는 총성 클릭 버스트 x5회. 각 이벤트를 12초
    버퍼 위 임의로 고른 시각에 겹쳐 그려서(더하기 믹싱) 불규칙하게 들리게 함.
  - 타이밍은 매번 랜덤이 아니라 코드에 고정된 시각표(`WAR_EXPLOSION_TIMES`,
    `WAR_GUN_BURSTS`) — 사용자가 "코드로 고정 타이밍이어도 된다"고 허용해서
    진짜 런타임 랜덤 대신 이 방식으로 감(구현이 훨씬 간단하고, 간격을
    불규칙하게 배치해서 청감상 랜덤처럼 들림).
  - `UpdateWarLoop`는 `SCENE_INTRO`/`SCENE_GAMEPLAY` 중이면서
    `finalResult != RESULT_HIT`일 때만 재생 -- 미션 성공 순간(RESULT_HIT
    확정 시점) 바로 멈춤. 실패 계열 결과(MISS/FIRE_FAIL/시간초과)는 명시적
    요청이 없어서 계속 울리다가, 결과 화면 홀드 끝나고 로비로 돌아가면
    (scene이 바뀌면서) 자연히 멈추고 BGM이 다시 시작됨.

## 조작 틱 사운드 + 3단계 좌클릭 전환 (2026-08-15, 같은 날 여덟 번째 세션)

- **레버/릴 드래그 틱 사운드**: "띠리릭 하는 소리" 요청. 4번째 waveOut 핸들
  (`g_waveOutTick`)을 새로 열어 SFX/BGM/War와 분리. `PlayTick()`은 950Hz
  사인을 20배속 감쇠(25ms)로 짧게 끊어 재생 -- 매 프레임 방위각 각도/사각
  값을 정수 밀 단위로 반올림해서 이전 프레임과 비교, **1밀 경계를 넘을
  때마다** 한 번씩 재생. 빠르게 드래그하면 자연히 여러 번 연달아 나서
  "띠리릭" 연속음처럼 들림 — 별도 타이머 없이 값 변화 자체를 트리거로 씀.
  방위각은 드래그 시작 시(`azLastTickMil`), 사각은 드래그 시작 + 관성
  슬라이드 구간(`elLastTickMil`) 둘 다에서 체크 -- 손 뗀 뒤 미끄러지는
  동안에도 계속 틱이 나야 "움직이는 동안"이라는 요청에 맞다고 판단함.
  우클릭 미세조정(1밀씩 딱딱 끊어지는 것)은 이미 그 자체로 완결된 액션이라
  틱 대상에서 제외.
- **3단계 명중 시 효과음**: `PlayHitSound()`가 500→1200Hz로 올라가는 차임을
  틱과 같은 `g_waveOutTick` 핸들로 재생(3단계 중엔 드래그를 안 해서 틱과
  절대 안 겹침 -- 핸들 재사용 안전).
- **3단계 조작을 스페이스 → 좌클릭으로 변경**: `mouseDown && !mouseDownPrev`로
  교체. 우측 게이지 위에 방위각/사각 스테이지와 같은 자리(안내문구 자리)에
  "좌클릭으로 발사" 추가, 하단 힌트도 "좌클릭으로 발사"로 문구 갱신.
  실제 좌클릭으로 판정(명중/실패)이 트리거되는 것까지 확인함(빗맞아서
  "사격 실패" 화면 정상 전환).

## 로비 메뉴 재설계 + 전쟁음 진짜 랜덤화 (2026-08-15, 같은 날 여섯 번째 세션)

- **전쟁 앰비언스를 고정 루프 → 진짜 런타임 랜덤 트리거로 전면 교체**: "너무 가볍다,
  저음으로, 랜덤성이 너무 없다"는 피드백. 기존 12초 고정 루프(`GenerateWarLoop`)를
  통째로 버리고, `PlayWarEvent()`가 이벤트 하나를 그 자리에서 즉석 합성해서
  한 방씩 재생하는 방식으로 바꿈 — `UpdateWarLoop(dt)`가 0.6~2.8초의 랜덤 간격마다
  호출. 종류 5가지 중 매번 랜덤 선택(원거리/근접 폭발, 단발/연발 총성, 초저역
  포성 rumble), 같은 종류라도 피치·길이·진폭을 매번 다시 뽑아서 반복감이 없게 함.
  저음 강화: 폭발 붐의 사인 성분을 16~30Hz(예전 25~55Hz보다 더 낮음)로, 노이즈보다
  사인 비중을 높임(0.78 vs 0.6). 총성도 노이즈 단독이 아니라 55~95Hz 저음 사인을
  섞어서 "탁" 대신 "퍽" 하는 무게감을 줌.
- **로비 메뉴 재설계**: 사용자가 새 로비 이미지(폭발 장면)를 준 김에 UI도 정리:
  - 옵션 버튼 → 우상단 톱니바퀴 아이콘(`DrawGearIcon`, 사각형 톱니 8개 + 원 몸통)으로
    교체. 클릭/호버 판정은 기존 버튼처럼 거리 기반 원형 히트테스트.
  - 시작 버튼 → "아무키나 눌러서 시작" 문구(화면 중앙에서 `GAME_H/2 + 30`만큼
    아래)로 교체. ESC를 제외한 아무 키 입력(`keys[i] && !keysPrev[i]`, i는
    VK_ESCAPE 제외 전체 순회)이 `SCENE_INTRO` 진입을 트리거.
  - 종료 버튼 삭제 → 로비에서 ESC 누르면 `running = FALSE`로 게임 자체를 닫음
    (`UpdateGame`의 `SCENE_LOBBY` 분기에 추가). 인트로/게임플레이의 기존 ESC
    동작(각각 무시/로비 복귀)과는 분리되어 있어 충돌 없음.
  - 옵션 오버레이가 떠 있을 때(`inputBlocked`)는 톱니바퀴 호버/클릭과 "아무키
    시작" 텍스트+감지 둘 다 비활성화 — 예전에 고쳤던 "팝업 뒤 텍스트 노출" 버그
    재발 방지 패턴을 그대로 적용.

## 전쟁음 다시 고정 루프로(더 촘촘하게) + 로비 미세조정 (2026-08-15, 같은 날 일곱 번째 세션)

- **전쟁 앰비언스: 런타임 랜덤 트리거 → 다시 고정 루프로, 대신 훨씬 촘촘하게**:
  "랜덤하게 하니까 긴장감이 떨어진다, 지정된 패턴으로, 공백 많으면 입체감
  떨어진다"는 피드백. 직전 세션에서 만든 `PlayWarEvent`(한 방씩 트리거) 방식의
  근본적 한계를 재발견함 -- 매번 `waveOutReset`으로 이전 소리를 끊고 새로
  트는 구조라 **이벤트끼리 절대 겹칠 수 없어서** 실제 전장처럼 여러 소리가
  동시에 깔리는 "입체감"을 낼 수 없었음. 그래서 6번째 세션 이전의 `GenerateWarLoop`
  (버퍼 하나에 여러 이벤트를 더하기 믹싱) 구조로 회귀하되:
  - 루프를 12초 → 16초로 늘리고 이벤트 수를 대폭 늘림(포성 rumble 2개(4~4.5초
    길이, 다른 레이어와 자유롭게 겹침) + 폭발 5회(원거리/근접 섞음) + 총성
    버스트 10회). 일부러 총성이 폭발 시작 시점과 겹치도록 배치(예: 총성
    6.2초 vs 폭발 6.5초)해서 레이어링 효과를 냄.
  - 저음 튜닝은 직전 세션 것을 그대로 가져옴(붐 16~30Hz 사인 위주, 총성도
    55~95Hz 저음 섞음) -- "가볍다"는 피드백에 대한 대응은 계속 유효하므로.
  - 타임라인(언제 무슨 이벤트)은 배열에 고정 하드코딩(="지정된 패턴")하되,
    각 이벤트 내부의 노이즈 텍스처·피치 지터는 여전히 `rand()`를 씀 -- 이건
    한 번(앱 실행 시 `GenerateWarLoop` 호출 시점)만 결정되고 그 뒤로는 루프
    내내 고정이라, "패턴 자체가 매번 바뀌는" 것과는 다름(요청과 안 어긋남).
  - `UpdateWarLoop`도 다시 dt 없는 폴링 방식으로 복귀(`WHDR_DONE`이면 같은
    버퍼 재큐잉) -- BGM과 동일한 패턴.
- **로비 "아무키나 눌러서 시작" 문구 위치**: 화면 중앙 근처(`GAME_H/2+30`) →
  세로 90% 지점(`GAME_H*9/10 - 12`, 하단 쪽)으로 이동.
- **로비 클릭으로도 시작 가능**: 기존엔 키보드 입력만 시작을 트리거했는데,
  마우스 클릭도 추가. 단, 톱니바퀴 아이콘 위 클릭은 옵션만 열고 시작에는
  안 걸리게 `!gearHover` 조건으로 분리(안 그러면 옵션 열면서 동시에 게임도
  시작되는 충돌 발생).
- **이미지 갱신 워크플로 정리**: `images/Lobby.bmp`(원본, 포토샵 저장 위치)를
  고치면 `build.sh`가 `dist/images/`로 복사해줘야 실제 게임에 반영됨 -- 자동
  동기화 아님. `dist/images/`를 직접 고치면 다음 빌드 때 원본 기준으로
  덮어써져서 조용히 사라지니, 항상 `images/`(원본)만 고치는 걸로 합의함.

## BMP 로더 + 로비 배경 아트 추가 (2026-08-15, 같은 날 네 번째 세션)

- **BMP 로더 구현**: `LoadBmpInto()`가 8비트 인덱스(팔레트) 또는 24비트 무압축
  (`BI_RGB`) BMP를 읽어 `GAME_W x GAME_H`(640x360) 크기의 `uint32_t` 픽셀
  배열로 채운다. 리사이즈는 안 해서 원본이 정확히 640x360이어야 함. 팔레트
  엔트리 수는 `biClrUsed`를 그대로 따름(256개 다 안 채워도 됨 — 실제로 로비
  배경은 64색 팔레트). Bottom-up/top-down(`biHeight` 부호) 둘 다 처리.
  `fopen`/`fread` 표준 C 파일 I/O 사용 — 추가 include는 `stdio.h`/`string.h`뿐이라
  실행파일 크기 영향은 미미함(31KB→33KB).
- **에셋 경로**: `GetModuleFileNameA`로 exe 자신의 경로를 구해서 그 옆의
  `images\` 폴더를 찾음 — 더블클릭 위치나 현재 작업 디렉터리에 안 좌우되게.
  `build.sh`가 `images/*.bmp`를 `dist/images/`로 같이 복사하도록 수정함
  (콘테스트 규정상 "배포 패키지 = 실행파일+에셋"이라 이렇게 나눠 배포하는 게
  허용됨 — `check_size.sh`도 원래 `dist/` 전체를 검사 대상으로 삼고 있었음).
- **로비 배경**: `images/Lobby.bmp` (640x360, 8비트 인덱스, K9 자주포 사격
  사진). 로드 실패 시(파일 없음 등) 기존 단색 placeholder로 자동 폴백하니
  에셋 없이도 빌드/실행 자체는 계속 됨. 사진이라 제목 텍스트 가독성을 위해
  타이틀 영역에 반투명 검정(45%)을 깔아둠 — 기존 보라색 placeholder 박스는
  배경 로드 성공 시엔 안 그림.
- **주의 -- 스케일링은 여전히 무보정**: 화면 출력은 전체 프레임버퍼를
  `StretchDIBits`로 한 번에 2배(`WINDOW_SCALE`) 확대하는 방식 그대로라
  (`SetStretchBltMode` 안 부름 = 블렌딩 없음), 픽셀아트뿐 아니라 사진도 같은
  방식으로 단순 복제 확대됨. 지금 로비 사진은 1280x720에서 봐도 크게 티 안
  나지만, 나중에 고해상도 사진을 쓰거나 부드러운 스케일링이 필요해지면
  배경 레이어만 별도로 `HALFTONE` 모드로 블릿하는 식의 분리가 필요할 수 있음
  (지금은 안 함 — 필요해지면 그때 다시 논의).
- **용량 확인**: dist/ 전체 264KB(exe 33KB + Lobby.bmp 230KB), zip 75KB.
  1.44MB 예산 대비 여유 충분(1.2MB+ 남음).

## 4단계 합산 판정으로 변경 + 레이아웃 정리 (2026-08-15, 같은 날 세 번째 세션)

- **콜아웃을 "장약 N 키로!!" 문장으로 교체**: 예전엔 다른 스테이지처럼 숫자 하나만
  띄웠는데, 이제 `wsprintfA` + `Kor()`로 조합한 문장을 씀. 문장이라 폭이 더
  필요해서 이 스테이지에서만 말풍선 박스를 110px→240px로 넓힘(`CHARGE_BUBBLE_*`).
- **콜아웃 사이 공백 시간 3배**: `BUBBLE_GAP_TIME`/`BUBBLE_PAUSE_TIME`을 그대로 쓰던
  걸 4단계 전용 `BUBBLE_PHASE_DUR_CHARGE` 배열로 분리하고 공백 구간만 3배로.
- **판정 방식을 "정확히 하나" → "통 안 원반들의 합"으로 변경**: 7이 목표면 7 하나를
  넣어도, 3+4를 넣어도, 1을 일곱 개 넣어도 됨. 특정 원반을 추적하는 대신, 놓을
  때마다(mouseUp) 통 사각형 안에 있는 모든 원반의 `chargeNumber` 합을 다시 계산해서
  목표와 비교 — 그래서 "제출됨" 상태 없이도 통에서 다시 꺼내면(드래그해서 밖으로)
  자동으로 합계에서 빠짐. 통 아래에 현재 합계(`합 N`)를 실시간으로 표시해서
  몇 개를 더 넣어야 할지 가늠할 수 있게 함.
- **무더기 레이아웃 조정**: 분포가 너무 넓어서(스프레드 150/90) 오른쪽 통 영역까지
  침범했던 문제 수정 — 중심을 왼쪽으로 옮기고(420→390) 스프레드를 좁힘(150/90→70/65).
  말풍선 박스가 넓어진 것과도 안 겹치게 여백을 계산해서 재배치.

## 후속 수정 + 4단계(장약 고르기) 추가 (2026-08-15, 같은 날 두 번째 세션)

- **한글 깜빡임 버그 수정**: 프레임버퍼(픽셀)와 GDI 텍스트를 화면 DC에 순서대로
  따로 그렸더니 그 사이 순간이 실제 화면에 노출되어 텍스트가 깜빡였음. 오프스크린
  메모리 DC(`g_hdc`)에 픽셀+텍스트를 전부 그린 뒤 `BitBlt`로 화면(`g_screenDC`)에
  한 번에 옮기는 더블 버퍼링으로 해결. (참고: 이 버그가 지난 세션에서 스크린샷에
  텍스트가 간헐적으로 안 보였던 것과 같은 원인이었음 — 그땐 캡처 타이밍 문제로
  오인했었음.)
- **무전 잡음 볼륨 50%로 축소**: `PlayStaticNoise`의 진폭에 0.5 곱함.
- **옵션 팝업 텍스트 겹침 버그 수정**: 텍스트는 픽셀 딤 처리와 별도 레이어라서,
  `SCENE_OPTIONS`에서 뒤쪽 로비를 그릴 때 `inputBlocked`여도 버튼 라벨 텍스트는
  큐에 계속 들어가고 있었음(픽셀은 딤되지만 텍스트는 그 위에 그대로 노출).
  `inputBlocked`면 아예 큐에 안 넣도록 수정.
- **인트로 ESC 제거**: 스페이스/클릭으로만 스킵. 대신 말풍선 우측 하단에 위아래로
  왕복하는 화살표(`DrawArrowDown` 신규)를 넣어서 "여기 클릭"을 시각적으로 알림.
- **2단계 릴 드래그 민감도 추가 완화**: `EL_DRAG_MIL_PER_PX` 1.0 → 0.5.
- **3단계 정리**: "명중 횟수" 문구/숫자 표시 제거 — 그 자리(중앙 숫자판)엔 이제
  아무 텍스트도 없고, 원래 있던 초록 사각 오브젝트가 자연스럽게 그 자리를 차지함.
  붉은 표시 폭을 실제 히트 판정 허용폭(`FIRE_MARK_TOLERANCE_PX`)의 2배로 그려서
  보이는 빨간 구간이 곧 정확한 히트 존이 되도록 하고, 불릿(6px)보다 확실히
  넓게(18px) 만듦.
- **4단계(장약 고르기) 신규 추가**: 무전으로 번호(1~5) 하나를 지정하면, 그 번호가
  적힌 원반 20개가 무더기로 겹쳐 쌓여있다 (`chargeX/Y/Number/Z`, z가 클수록 위).
  드래그로 집으면 그 원반의 z가 즉시 최상단으로 갱신되어 위로 올라오고(무더기를
  "치우는" 느낌), 마우스를 놓으면 그 자리에 그대로 남는다 — 이게 곧 "찾기" 메커닉.
  목표 번호와 맞는 원반을 화살표가 위아래로 움직이는 통(`CHARGE_BIN_*`)에 놓으면
  스테이지 종료. 틀린 번호를 통에 놓아도 페널티 없이 그냥 그 자리에 남을 뿐
  (순수 탐색 퍼즐, 시간 제한 외엔 실패 조건 없음). 목표 번호가 무더기 안에
  최소 1개는 반드시 존재하도록 인덱스 0에 강제로 심어둠 (안 그러면 20개 다
  각자 독립 랜덤이라 운 나쁘면 목표가 무더기에 아예 없을 수 있음 — 확률은 낮지만).
  스테이지 순서는 방위각 → 사각 → 사격 → **장약** → 최종판정으로 바뀜. 3단계에서
  타이밍 실패(`fireStageFailed`)하면 4단계로 안 넘어가고 그 자리에서 바로
  게임오버(`RESULT_FIRE_FAIL`)로 처리 — 4단계는 3단계를 통과한 사람만 봄.
- **로컬 테스트 팁**: 4단계처럼 뒤쪽 스테이지를 매번 처음부터 플레이해서
  검증하기 번거로우면, `EnterGameplay()`에서 `fireStage` 초기값과 해당 스테이지의
  `Enter*Stage()` 호출을 잠깐 바꿔서 바로 진입시켜 확인한 뒤 되돌리는 방법을 씀
  (이번 세션에서 4단계 검증할 때 이렇게 함 — 커밋 전에 반드시 원상복구 확인).

## 한글 전환 + 3단계 사격 추가 (2026-08-15)

- **한글 텍스트 렌더링 방식 결정**: 5x7 도트매트릭스 폰트는 영문 A-Z만 가능해서
  한글이 원천적으로 불가능(조합형 11,172자를 손으로 그릴 수 없음). 대신
  **프레임버퍼를 화면에 낸 직후, 같은 HDC 위에 GDI `TextOutW`/`DrawTextW`로
  시스템 폰트(맑은 고딕)를 덧그리는 방식**으로 교체. 외부 폰트 파일 없이
  Windows 기본 폰트를 쓰므로 용량 영향 없음(exe는 여전히 ~29KB). `QueueTextPoint`/
  `QueueTextCentered`로 한 프레임 분량 텍스트를 큐에 모았다가 `StretchDIBits`
  직후 `FlushTextQueue`에서 한꺼번에 그림. 기존 5x7 폰트 관련 코드(`FONT5X7`,
  `DrawChar`, `DrawLabel`, `TextWidth`)는 전부 삭제.
- **문자열 인코딩 주의**: 소스는 UTF-8인데 `L"한글"` 와이드 리터럴을 mingw
  크로스컴파일 환경에서 그대로 쓰면 인코딩이 꼬일 위험이 있어서, 전부
  `Kor("UTF-8 문자열")` 헬퍼(내부에서 `MultiByteToWideChar(CP_UTF8,...)`)를
  통해 런타임에 명시적으로 변환하는 방식으로 통일함. `Kor()`는 4칸짜리
  로테이팅 버퍼를 씀 — "만들고 바로 다음 줄에서 소비" 패턴을 지키는 한 안전.
- **버튼 시스템도 이 위에서 다시 씀**: `Button.label`이 `const char*`에서
  `const wchar_t*`로 바뀌었고, 가운데 정렬은 예전처럼 `TextWidth`로 픽셀폭을
  직접 계산하지 않고 `DrawTextW`의 `DT_CENTER|DT_VCENTER`에 맡김 (한글은
  글자폭이 일정하지 않아서 수동 계산이 더 위험함).
- **말풍선 "치직" 인트로 연출**: 스테이지(방위각/사각) 진입 직후 핑크 배경만
  0.08초 간격으로 3번 깜빡이고(`bubbleIntroActive`/`BUBBLE_INTRO_FLASH_*`),
  끝나야 원래 있던 자릿수 콜아웃 사이클이 시작됨. 3단계(사격)에는 이 말풍선
  자체가 없음.
- **오디오 첫 도입**: `waveOut` API로 8비트 PCM 백색잡음 + 랜덤 팝(pop)을
  즉석에서 합성해 "지직" 효과음으로 씀(`PlayStaticNoise`). 샘플 WAV를 담지
  않아 외부 의존성 0 원칙 유지. 말풍선 치직 연출과 같은 타이밍에 재생.
  `soundOn` 옵션을 존중함.
- **커스텀 픽셀 커서**: 시스템 커서는 `WM_SETCURSOR`에서 숨기고
  (`wc.hCursor = NULL` + `SetCursor(NULL)`), 프레임버퍼에 직접 십자 모양을
  그림. 클릭/드래그 가능 지점 위에서는 노란색, 아니면 회색 (`cursorHot`
  플래그 — `UpdateGame` 시작 시 리셋하고 버튼 hover·그랩존 판정에서 세움).
- **옵션 팝업 입력 차단 버그 수정**: 원래 `SCENE_OPTIONS`일 때도 뒤쪽
  `RenderLobby()`를 그대로 다시 그리면서 그 안의 `ButtonClicked`가 실제로
  클릭 판정까지 했음(팝업 뒤 로비 버튼이 눌리는 버그). `inputBlocked` 플래그로
  옵션이 뜬 동안만 로비 렌더를 "그리기 전용"으로 강제하고, 그 위에 반투명
  검정(`FillPixelRectAlpha` 55%)을 깔아 딤 처리.
- **3단계(사격 타이밍) 신규 추가**: 방위각·사각 확정 후 `FIRESTAGE_FIRE`로
  전환. 좌측 사각 오브젝트 + 우측 게이지(불릿이 왕복, 붉은 표시는 명중마다
  새 위치로 랜덤). 표시와 불릿이 겹칠 때 스페이스 = 명중(오브젝트가 왼쪽으로
  이동, 다음 라운드는 불릿 속도가 약간 빨라짐), 빗맞은 타이밍에 스페이스 =
  즉시 실패(`RESULT_FIRE_FAIL`, "사격 실패" 화면). 5회 명중하면 기존
  방위각+사각 오차 합산 판정으로 최종 명중/실패가 갈림 — 즉 3단계를 통과해도
  1·2단계에서 오차가 컸으면 "임무 실패"로 끝날 수 있음(의도된 동작).
- **방위각 기어비 20 → 15바퀴**로 완화 (`AZ_GEAR_TURNS`).
- **테스트 방법 메모**: 로컬(MSYS2 mingw-w64)에서 빌드한 뒤, 창을 포커스하지
  않고도 `PostMessage`로 클릭/키 입력을 보내고 `PrintWindow`로 캡처하면
  다른 창에 포커스를 뺏겨도 안정적으로 화면을 검증할 수 있음. 단, 창이
  최소화(`IsIconic`)되면 `PrintWindow`가 마지막 프레임에서 멈춘 정지 이미지를
  반환하니 캡처 전에 항상 복원(`ShowWindowAsync(hwnd, SW_RESTORE)`)해야 함.

## 작업 방식 / git 브랜치 규칙

- 개발 브랜치: **`claude/144mb-game-plan-juicn3`** (이 저장소의 기본 작업 브랜치)
- `main`은 이 브랜치를 fast-forward 머지해서 최신 상태 유지 (필요할 때마다 수동으로)
- **동기화되는 건 git뿐, 대화 맥락은 동기화 안 됨** — 웹 세션과 로컬 세션은 서로
  다른 대화라 이 파일 + 커밋 로그가 유일한 연결고리. 새 세션 시작할 땐 이 문서와
  `git log --oneline`을 먼저 읽는 걸 추천.
- 커밋마다 "뭘 왜 바꿨는지" 최대한 자세히 적어둠 — `git log`만 봐도 웬만한 맥락은
  따라올 수 있게.
